@model IEnumerable<Birder2.Models.Bird>

@{
    ViewData["Title"] = "Birds";
    Layout = "~/Views/Shared/_LayoutNoSidebar.cshtml";
     int counter = 0;
}

<h2>British Bird Species</h2>

<form asp-action="Index" method="get">
    <div class="form-actions no-color">
        <p>
            Find by name: <input type="text" name="SearchString" />
            <input type="submit" value="Search" class="btn btn-default" /> |
            <a asp-action="Index">Back to Full List</a>
        </p>
    </div>
</form>
@* Change to view model to include a list of bird family categories!
    <form asp-controller="Home" asp-action="Index" method="post">
        <select asp-for="Country" asp-items="Model.Countries"></select>
        <br /><button type="submit">Register</button>
    </form>*@


@*<form asp-action="Index" method="get">
        <div class="row">
            <div class="col-lg-6">
                <div class="input-group">
                    <div class="input-group-btn">
                        <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Action <span class="caret"></span></button>
                        <ul class="dropdown-menu-right">
                            @*<li><a href="#">Action</a></l
                            <li><a asp-action="Details" asp-route-id="1">Another action</a></li>
                            <li><a href="#">Something else here</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="#">Separated link</a></li>
                        </ul>
                    </div><!-- /btn-group -->
                    <input type="text" name="SearchString" class="form-control" aria-label="...">
                </div><!-- /input-group -->
            </div><!-- /.col-lg-6 -->
        </div><!-- /.row -->
    </form>*@

<div class="row">
    @foreach (var item in Model)
    {
        if (counter != 0 && counter % 3 == 0)
        {
        @:</div>
        @:<div class="row">
        }

        <div class="col-md-4">
            <div class="thumbnail">
                @* style="background-color: seagreen"> *@
                <a href="Bird/Details/@item.BirdId" />
                <img src="~/images/if_099374-twitter-bird3_56010.png" alt="ASP.NET" class="img-responsive" />

                <div class="caption">
                    <h3 id="thumbnail-label">@Html.DisplayFor(modelItem => item.EnglishName)</h3>
                    <p><i>@Html.DisplayFor(modelItem => item.ScientificName)</i></p>

                    @*<h3 style='display:inline'>@Html.DisplayFor(modelItem => item.EnglishName)</h3><p style='display:inline'><i>   @Html.DisplayFor(modelItem => item.ScientificName)</i></p>*@
                </div>
            </div>
        </div>

        counter++;
    }
</div>



@*


    @model IEnumerable<Birder2.Models.Bird>

    @{
        ViewData["Title"] = "Index";
    }

    <h2>Index</h2>

    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Image)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.EnglishName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.InternationalName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ScientificName)
                    </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
    @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Image)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EnglishName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.InternationalName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ScientificName)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.BirdId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.BirdId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.BirdId">Delete</a>
                </td>
            </tr>
    }
        </tbody>
    </table>

*@
