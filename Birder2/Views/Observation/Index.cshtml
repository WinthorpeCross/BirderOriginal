@model IEnumerable<Birder2.Models.Observation>

@{
    ViewData["Title"] = "My Observations";
}

<h2>Observations [sightings] feed</h2>

<div>
    <p>
        <a asp-action="Create">Post an Observation</a>
    </p>
</div>


@*<form asp-action="Index" asp-route-filter="myselect" class="form-horizontal">
    <select name="myselect" onchange="this.form.submit(myselect.submit)">
        <option value="1">One</option>
        <option value="2">Two</option>
        <option value="3">Three</option>
        <option value="4">Four</option>
    </select>*@
<form asp-controller="Observation" asp-action="Index" method="post" class="form-horizontal">
    <div class="form-group">
        <div class="col-sm-4">
            <label class="control-label">Sort By</label>
            <select name="filter" onchange="this.form.submit()" class="form-control sort-filter-width">
                <option selected="selected" value="">Choose...</option>
                <option value="">My Network And Me</option>
                <option value="MyNetworkOnly">My Network Only</option>
                <option value="MineOnly">Mine Only</option>
            </select>
            </div>
        </div>

            @*<input type="submit" value="Filter" />*@


            @*<select name="filter" input type="submit" value="Filter">
                    <option value="h">All</option>

                </select>*@



            @*<form asp-action="Index" asp-route-searchString="SearchString" asp-route-searchType="@ViewData["searchType"]" method="get">
                    <div class="form-actions no-color">
                        <p>
                            Find by name: <input type="text" name="SearchString" />
                            <input type="submit" value="Search" class="btn btn-default" /> |
                            <a asp-action="Index" asp-route-searchType="@ViewData["searchType"]">Back to Full List</a>
                        </p>
                    </div>
                </form>*@
            @*<div class="form-group">
                    <div class="col-sm-3">
                        <label class="control-label">Filter Type</label>
                        <select name="FilterBy" class="form-control sort-filter-width" onchange="this.form.submit()" data-val="true" data-val-required="The FilterBy field is required." id="SortFilterPageData_FilterBy">
                            <option selected="selected" value="">My Network And Me</option>
                            <option value="MyNetworkOnly">My Network Only</option>
                            <option value="MineOnly">Mine Only</option>
                        </select>
                    </div>
                </div>*@

            @*<div class="col-sm-3 dim-filter-value" id="filter-value-group">
                        <label class="control-label">Filter By</label>
                        <select name="FilterValue" id="filter-value-dropdown" class="form-control sort-filter-width" disabled onchange="BookList.sendForm(this)">
                            <option>Select filter type...</option>
                        </select>
                    </div>

                    <div class="col-sm-2">
                        <label class="control-label">Page</label>
                        <div>
                            <input name="PageNum" class="form-control page-control-width page-num-input" onchange="BookList.sendForm(this)" type="number" data-val="true" data-val-required="The PageNum field is required." id="SortFilterPageData_PageNum" value="1">
                            <span class="num-pages-text">of 10,000</span>
                        </div>
                    </div>

                    <div class="col-sm-1">
                        <label class="control-label">Page&nbsp;Size</label>
                        <select name="PageSize" class="form-control page-control-width" onchange="BookList.sendForm(this)" data-val="true" data-val-required="The PageSize field is required." id="SortFilterPageData_PageSize">
                            <option value="5">5</option>
                            <option selected="selected" value="10">10</option>
                            <option value="20">20</option>
                            <option value="50">50</option>
                            <option value="100">100</option>
                            <option value="500">500</option>
                            <option value="1000">1000</option>
                        </select>
                    </div>
                </div>

                <input id="PrevCheckState" name="PrevCheckState" type="hidden" value=0,,10,10000>*@
</form>
<br />


@foreach (var observation in Model)
{
    <div class="container">
        @Html.Partial("_Observations", observation)
        @*<hr />*@
    </div>
}
