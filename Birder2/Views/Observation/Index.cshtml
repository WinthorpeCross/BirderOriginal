@model IEnumerable<Birder2.Models.Observation>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ObservationDateTime)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Location)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Note)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ApplicationUserId)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Bird)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ObservationDateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Note)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ApplicationUserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bird.EnglishName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ObservationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ObservationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ObservationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
